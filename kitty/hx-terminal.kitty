#!/usr/bin/env bash
cd $1
if [ $? -ne 0 ]; then
	exit 1;
fi

kitty @ set-tab-title $(basename $(pwd))

kitty @ launch --cwd $(pwd) > /dev/null
kitty @ focus-window --match 'recent:1'

kitty @ send-text "cd $(pwd)\nhx\n"
